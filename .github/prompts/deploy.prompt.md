---
description: "Deploy to Vercel production. Use when: deploy, push to production, ship it."
---

# Deploy to Production

Deploy the current codebase to Vercel production. Run the command directly — no confirmation needed.

```bash
npx vercel --prod
```

After deployment completes, report the production URL from the output.
