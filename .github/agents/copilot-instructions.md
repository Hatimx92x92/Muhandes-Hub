# Muqawilhub - Local Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-03

## Active Technologies
- TypeScript (strict, no `any`) on Next.js 16 with React 19 + `@supabase/ssr`, `zod`, `@moyasar/moyasar-react` (or raw API), `@react-pdf/renderer`, Supabase (010-payments-subscriptions)
- Supabase PostgreSQL (5 new tables) + Supabase Storage (1 new bucket) (010-payments-subscriptions)

- TypeScript (strict, no `any`) on Next.js 16 with React 19 + `@supabase/ssr`, `@supabase/supabase-js`, `next-intl`, `zod`, `@upstash/ratelimit`, `@upstash/redis` (001-platform-foundation)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

TypeScript (strict, no `any`) on Next.js 16 with React 19: Follow standard conventions

## Recent Changes
- 010-payments-subscriptions: Added TypeScript (strict, no `any`) on Next.js 16 with React 19 + `@supabase/ssr`, `zod`, `@moyasar/moyasar-react` (or raw API), `@react-pdf/renderer`, Supabase

- 001-platform-foundation: Added TypeScript (strict, no `any`) on Next.js 16 with React 19 + `@supabase/ssr`, `@supabase/supabase-js`, `next-intl`, `zod`, `@upstash/ratelimit`, `@upstash/redis`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
